const database = require("../database")
const userModel = {
    validLocalUser: (email, password) => {
        user = {
            id: 1,
            name: "cindy",
            password: "cindy",
        };
        return user;
    }

}

module.exports = userModel;